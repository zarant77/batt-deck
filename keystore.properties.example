# Copy this file to keystore.properties and fill in the real local secrets.
# Absolute paths are recommended so the key can live outside the repository.
# Relative paths are resolved from the project root.
storeFile=/absolute/path/to/catemup-upload.jks
storePassword=CHANGE_ME
keyAlias=CHANGE_ME
keyPassword=CHANGE_ME
